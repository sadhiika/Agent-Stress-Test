# Core (needed for the offline stub run)
pydantic>=2.5
python-dotenv>=1.0

# Live target calls
httpx>=0.27

# Live providers (only needed when JUDGE_PROVIDER/GENERATOR_PROVIDER=vertex)
google-cloud-aiplatform>=1.60
# vertexai is included in google-cloud-aiplatform

# Observability (only needed when ENABLE_TRACING=1)
langfuse>=2.0

# Dev
pytest>=8.0
ruff>=0.5

google-genai
